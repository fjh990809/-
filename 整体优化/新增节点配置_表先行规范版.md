# 新增节点配置_表先行规范版

本文只写这次新增的节点配置，不展开旧节点。

## 1. `Agent:TableScopeSelect`

- 节点名称：`表选择分析器_非预置问题`
- 节点类型：`agentNode`
- 模型：`Qwen3-32B___OpenAI-API@OpenAI-API-Compatible`
- 结构化输出：开启
- 入边：
  - `Switch:ThreeCrewsStay(end_cpn_ids) -> Agent:TableScopeSelect(end)`
  - `Retrieval:SlowCamerasOccur(start) -> Agent:TableScopeSelect(end)`
  - `Retrieval:FloppyClothsItch(start) -> Agent:TableScopeSelect(end)`
  - `Retrieval:ModernBirdsRelate(start) -> Agent:TableScopeSelect(end)`

### user prompt

```text
用户问题：{Agent:ElevenThingsChange@content}
表字段描述：{Retrieval:SlowCamerasOccur@formalized_content}
建表语句：{Retrieval:FloppyClothsItch@formalized_content}
SQL问答对样例：{Retrieval:ModernBirdsRelate@formalized_content}
```

### sys prompt

- 文件：[01_表选择分析器_sys_prompt.txt](/Users/jhf/Downloads/数据管家/问数/整体优化/prompts/01_表选择分析器_sys_prompt.txt)

### 最小 structured output schema

```json
{
  "tables": ["表名1", "表名2"]
}
```

### RAGFlow 结构化配置

```json
{
  "properties": {
    "tables": {
      "description": "当前问题生成SQL时应优先使用的表名数组",
      "type": "array"
    }
  },
  "required": ["tables"]
}
```

## 2. `Iteration:TableDictLoop`

- 节点名称：`按表迭代DICT_非预置问题`
- 节点类型：`group`
- 作用：把 `表选择分析器` 输出的 `tables` 数组逐个送入 DICT 检索和字典裁剪
- 入边：
  - `Agent:TableScopeSelect(start) -> Iteration:TableDictLoop(end)`

### 组节点配置

```json
{
  "items_ref": "Agent:TableScopeSelect@structured.tables",
  "outputs": {
    "dicts1": {
      "ref": "Agent:DictFieldTrim@structured",
      "type": "Array<Object>"
    }
  }
}
```

### 内部子节点 1: `IterationItem:TableDictItem`

```json
{
  "outputs": {
    "index": { "type": "integer" },
    "item": { "type": "unkown" }
  }
}
```

### 内部子节点 2: `Retrieval:TableDictRetrieve`

- 节点名称：`DICT检索_按表_非预置问题`
- 节点类型：`retrievalNode`
- 知识库：只用当前 `DICT`
- `kb_ids`：`["705e7a441b9611f18db3e50efcb1c622"]`

#### query

```text
{IterationItem:TableDictItem@item}
```

#### 核心参数

```json
{
  "empty_response": "未查询到相关字典信息",
  "keywords_similarity_weight": 1,
  "similarity_threshold": 0.2,
  "top_k": 1024,
  "top_n": 4,
  "use_kg": false,
  "toc_enhance": false
}
```

## 3. `Agent:DictFieldTrim`

- 节点名称：`字典裁剪器_非预置问题`
- 节点类型：`agentNode`
- 模型：`Qwen3-32B___OpenAI-API@OpenAI-API-Compatible`
- 结构化输出：开启
- 入边：
  - `Retrieval:TableDictRetrieve(start) -> Agent:DictFieldTrim(end)`

### user prompt

```text
用户问题：{Agent:ElevenThingsChange@content}
当前表名：{IterationItem:TableDictItem@item}
当前表字典内容：{Retrieval:TableDictRetrieve@formalized_content}
建表语句：{Retrieval:FloppyClothsItch@formalized_content}
表字段描述：{Retrieval:SlowCamerasOccur@formalized_content}
```

### sys prompt

- 文件：[02_字典裁剪器_sys_prompt.txt](/Users/jhf/Downloads/数据管家/问数/整体优化/prompts/02_字典裁剪器_sys_prompt.txt)

### 最小 structured output schema

```json
{
  "table_name": "表名",
  "dict_fields": [
    {
      "field_name": "字段名",
      "mapping": "字段对应的字典原文"
    }
  ]
}
```

### RAGFlow 结构化配置

```json
{
  "properties": {
    "table_name": {
      "description": "当前字典裁剪结果所属表名",
      "type": "string"
    },
    "dict_fields": {
      "description": "当前问题真正需要用于条件值转换的字段字典",
      "type": "array"
    }
  },
  "required": ["table_name", "dict_fields"]
}
```

## 4. 新增节点之外，必须同步调整的旧节点

这里只列必要项，不展开旧节点全量配置。

- `Agent:ElevenThingsChange`
  - 作用：
    - 作为 `DB / DDL / Q-SQL` 检索查询改写器，只补足上下文，不改变实体值
    - 支持 `sys.history` 多轮承接
    - 通过“简称映射检索工具”支持《新博数据管家_简称映射》中的简称补全
  - `sys prompt` 文件：
    - [00_非预置问题改写器_sys_prompt.txt](/Users/jhf/Downloads/数据管家/问数/整体优化/prompts/00_非预置问题改写器_sys_prompt.txt)
  - `user prompt`：

```text
【当前用户问题】
{sys.query}

【对话历史】
{sys.history}
```

  - 若给该节点挂简称映射检索工具，工具描述建议使用：
    - [00_简称映射检索工具_description.txt](/Users/jhf/Downloads/数据管家/问数/整体优化/prompts/00_简称映射检索工具_description.txt)

- `Agent:SilverRocketsPoke`
  - `user prompt` 改为同时消费：
    - `用户问题`
    - `表选择结果`
    - `建表语句`
    - `表字段描述`
    - `SQL问答对样例`
    - `按表裁剪后的字典值`
  - `sys prompt` 文件：
    - [03_非预置SQL生成器_sys_prompt.txt](/Users/jhf/Downloads/数据管家/问数/整体优化/prompts/03_非预置SQL生成器_sys_prompt.txt)
  - 删除 `tools`
- `Agent:SweetJokesHear`
  - 用户提示词补一行：

```text
字段字典值：{Iteration:DullBagsYawn@dicts1}
```
