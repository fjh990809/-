CREATE TABLE `ldly_base_d_aio` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备ID',
  `device_name` varchar(100)  DEFAULT NULL COMMENT '设备名称',
  `road_no` varchar(32)  DEFAULT NULL COMMENT '路段号',
  `national_road_no` int DEFAULT NULL COMMENT '国家高速编号',
  `station_no` varchar(32)  DEFAULT NULL COMMENT '设施编码',
  `aio_type` int DEFAULT NULL COMMENT '一体机类型',
  `ip` varchar(100)  DEFAULT NULL COMMENT 'ip地址',
  `api_address` varchar(200)  DEFAULT NULL COMMENT '管理平台接口地址',
  `is_active` int DEFAULT NULL COMMENT '是否启用',
  `is_approving` int DEFAULT NULL COMMENT '是否审批中',
  `is_delete` int DEFAULT NULL COMMENT '是否删除',
  `create_time` varchar(30)  DEFAULT NULL COMMENT '保存时间',
  `update_time` varchar(30)  DEFAULT NULL COMMENT '最近更新时间',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`device_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运一体机表，用于记录路运一体机的设备编号、设备名称、路段号、国家高速编号、设施编码、类型、IP地址、管理平台接口地址、启用状态、创建及更新时间等基础信息和状态信息，支持按设备编号、设备名称、路段号、国家高速编号、设施编码、类型、IP地址等多维度精确或模糊查询，支持按启用状态、创建/更新时间、数据状态等条件筛选，支持统计一体机总数、按路段号、类型、国家高速编号分类统计，支持查询最新创建或更新的设备，适用于设备信息管理、状态监控及数据分析:';
CREATE TABLE `ldly_base_d_alarm_host` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备ID',
  `device_name` varchar(100)  DEFAULT NULL COMMENT '设备名称',
  `master_type` int DEFAULT NULL COMMENT '类型',
  `protocol_flag` int DEFAULT NULL COMMENT '通讯方式',
  `protocol` varchar(50)  DEFAULT NULL COMMENT '控制协议',
  `ip` varchar(100)  DEFAULT NULL COMMENT 'IP地址',
  `port` int DEFAULT NULL COMMENT '端口',
  `com_port` int DEFAULT NULL COMMENT '串口号',
  `com_baud` int DEFAULT NULL COMMENT '波特率',
  `user_name` varchar(100)  DEFAULT NULL COMMENT '用户名',
  `password` varchar(100)  DEFAULT NULL COMMENT '密码',
  `road_no` varchar(32)  DEFAULT NULL COMMENT '路段号',
  `national_road_no` int DEFAULT NULL COMMENT '国家高速编号',
  `direction_no` int DEFAULT NULL COMMENT '方向编码',
  `station_no` varchar(32)  DEFAULT NULL COMMENT '设施编码',
  `mile_post` varchar(50)  DEFAULT NULL COMMENT '桩号',
  `mp_value` bigint DEFAULT NULL COMMENT '桩号数值',
  `is_active` int DEFAULT NULL COMMENT '是否启用',
  `is_approving` int DEFAULT NULL COMMENT '是否审批中',
  `is_delete` int DEFAULT NULL COMMENT '是否删除',
  `create_time` varchar(30)  DEFAULT NULL COMMENT '保存时间',
  `update_time` varchar(30)  DEFAULT NULL COMMENT '最近更新时间',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`device_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运报警主机表，用于记录报警主机的设备编号、设备名称、类型、通讯方式、控制协议、IP地址、端口、串口号、波特率、用户名、路段号、国家高速编号、方向编码、设施编码、桩号及数值等基础信息，支持按设备编号、IP地址、路段号、方向编码、桩号、通讯方式、控制协议、设施编码等多维度查询，支持启用状态（已启用、未启用、审批中）筛选，支持统计报警主机总数及按类型、通讯方式、路段号、方向编码分类统计，关联广播、紧急电话、火灾报警器、动环设备等子设备信息，支持查询子设备及其所属主机详情，支持模糊查询设备名称（如“九连山”），并可统计各类子设备数量及启用状态:';
CREATE TABLE `ldly_base_d_broadcast` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备ID',
  `device_name` varchar(100)  DEFAULT NULL COMMENT '设备名称',
  `alarm_host_id` varchar(32)  DEFAULT NULL COMMENT '所属报警主机',
  `address` varchar(200)  DEFAULT NULL COMMENT '设备地址码',
  `road_no` varchar(32)  DEFAULT NULL COMMENT '路段号',
  `national_road_no` int DEFAULT NULL COMMENT '国家高速编号',
  `station_no` varchar(32)  DEFAULT NULL COMMENT '设施编码',
  `direction_no` int DEFAULT NULL COMMENT '方向编码',
  `mile_post` varchar(50)  DEFAULT NULL COMMENT '桩号',
  `mp_value` bigint DEFAULT NULL COMMENT '桩号数值',
  `x` bigint DEFAULT NULL COMMENT '经度',
  `y` bigint DEFAULT NULL COMMENT '纬度',
  `is_active` int DEFAULT NULL COMMENT '是否启用',
  `is_approving` int DEFAULT NULL COMMENT '是否审批中',
  `is_delete` int DEFAULT NULL COMMENT '是否删除',
  `create_time` varchar(30)  DEFAULT NULL COMMENT '保存时间',
  `update_time` varchar(30)  DEFAULT NULL COMMENT '最近更新时间',
  `broadcast_type` int DEFAULT NULL COMMENT '广播类型',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`device_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运广播表，用于记录广播设备的基础信息、位置信息、状态信息及与报警主机的关联关系，支持按设备编号、设备名称、所属报警主机、路段号、国家高速编号、设施编码、方向编码、桩号、经纬度、广播类型等多维度查询和筛选，支持启用状态、广播类型、路段号、方向编码、报警主机等分类统计，可查询设备详细信息及关联报警主机信息，支持在线/离线/故障状态统计及最新更新记录查询，支持按设备地址码、桩号范围、IP地址等条件进行精准检索，适用于广播设备全生命周期管理和运行状态监控:';
CREATE TABLE `ldly_base_d_camera` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备ID',
  `device_name` varchar(100)  DEFAULT NULL COMMENT '设备名称',
  `road_no` varchar(32)  DEFAULT NULL COMMENT '路段号',
  `national_road_no` int DEFAULT NULL COMMENT '国家高速编号',
  `direction_no` int DEFAULT NULL COMMENT '方向编码',
  `station_no` varchar(32)  DEFAULT NULL COMMENT '设施编码',
  `mile_post` varchar(50)  DEFAULT NULL COMMENT '桩号',
  `mp_value` bigint DEFAULT NULL COMMENT '桩号数值',
  `x` bigint DEFAULT NULL COMMENT '经度',
  `y` bigint DEFAULT NULL COMMENT '纬度',
  `is_active` int DEFAULT NULL COMMENT '是否启用',
  `is_approving` int DEFAULT NULL COMMENT '是否审批中',
  `is_delete` int DEFAULT NULL COMMENT '是否删除',
  `create_time` varchar(30)  DEFAULT NULL COMMENT '保存时间',
  `update_time` varchar(30)  DEFAULT NULL COMMENT '最近更新时间',
  `factory` int DEFAULT NULL COMMENT '摄像枪厂家',
  `camera_type` int DEFAULT NULL COMMENT '摄像枪类型',
  `ipc_ip` varchar(50)  DEFAULT NULL COMMENT '摄像枪IP',
  `ipc_port` int DEFAULT NULL COMMENT '摄像枪端口',
  `ipc_profile` varchar(100)  DEFAULT NULL COMMENT '摄像枪Profile',
  `encoder_id` varchar(32)  DEFAULT NULL COMMENT '编码器',
  `encoder_channel` int DEFAULT NULL COMMENT '编码器通道',
  `ipc_user_name` varchar(50)  DEFAULT NULL COMMENT '用户名',
  `ipc_password` varchar(50)  DEFAULT NULL COMMENT '密码',
  `ipc_channel` int DEFAULT NULL COMMENT 'IPC通道',
  `dvr_id` varchar(32)  DEFAULT NULL COMMENT '存储设备',
  `dvr_channel` varchar(100)  DEFAULT NULL COMMENT '存储设备通道',
  `soft_protocol` varchar(50)  DEFAULT NULL COMMENT '播放协议',
  `support_ptz` int DEFAULT NULL COMMENT '是否支持云台',
  `ptz_protocol` varchar(50)  DEFAULT NULL COMMENT '云台控制协议',
  `ptz_address` varchar(200)  DEFAULT NULL COMMENT '云台控制地址',
  `master_url` varchar(500)  DEFAULT NULL COMMENT '主码流',
  `second_url` varchar(500)  DEFAULT NULL COMMENT '副码流',
  `third_url` varchar(500)  DEFAULT NULL COMMENT '第三码流',
  `nvr_master_url` varchar(200)  DEFAULT NULL COMMENT 'nvr主码流',
  `nvr_second_url` varchar(200)  DEFAULT NULL COMMENT 'nvr辅码流',
  `nvr_third_url` varchar(200)  DEFAULT NULL COMMENT 'nvr第三码流',
  `support_mul` int DEFAULT NULL COMMENT '是否支持组播',
  `mul_ip` varchar(50)  DEFAULT NULL COMMENT '视频组播IP',
  `mul_port` int DEFAULT NULL COMMENT '视频组播端口',
  `support_audio` int DEFAULT NULL COMMENT '是否支持音频',
  `mul_audio_ip` varchar(50)  DEFAULT NULL COMMENT '音频组播IP',
  `mul_audio_port` int DEFAULT NULL COMMENT '音频组播端口',
  `city` varchar(50)  DEFAULT NULL COMMENT '地级市',
  `install_position` varchar(200)  DEFAULT NULL COMMENT '安装位置',
  `is_out` int DEFAULT NULL COMMENT '是否上报集团',
  `tvwall_channel` varchar(200)  DEFAULT NULL COMMENT '设备源通道',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`device_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运IPC摄像枪表，用于记录路运系统中所有摄像枪的设备编号、设备名称、路段号、国家高速编号、方向编码、设施编码、桩号、经纬度、厂家、类型、IP地址、端口、Profile、编码器信息、用户名、IPC通道、存储设备信息、播放协议、云台控制协议、云台控制地址、主码流、副码流、第三码流、NVR主码流、NVR辅码流、NVR第三码流、视频组播IP、视频组播端口、音频组播IP、音频组播端口、地级市、安装位置、设备源通道、是否启用、是否上报集团等详细信息，支持按设备编号、路段号、国家高速编号、方向、桩号、地级市等多维度查询，可区分摄像枪类型（枪机/球机）、是否支持云台/音频/组播，支持查询九连山隧道、南行、北行等特定路段摄像枪的详细配置信息:';
CREATE TABLE `ldly_base_d_cms` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备ID',
  `device_name` varchar(100)  DEFAULT NULL COMMENT '设备名称',
  `road_no` varchar(32)  DEFAULT NULL COMMENT '路段号',
  `national_road_no` int DEFAULT NULL COMMENT '国家高速编号',
  `direction_no` int DEFAULT NULL COMMENT '方向编码',
  `station_no` varchar(32)  DEFAULT NULL COMMENT '设施编码',
  `mile_post` varchar(50)  DEFAULT NULL COMMENT '桩号',
  `mp_value` bigint DEFAULT NULL COMMENT '桩号数值',
  `x` bigint DEFAULT NULL COMMENT '经度',
  `y` bigint DEFAULT NULL COMMENT '纬度',
  `is_approving` int DEFAULT NULL COMMENT '是否审批中',
  `is_delete` int DEFAULT NULL COMMENT '是否删除',
  `create_time` varchar(30)  DEFAULT NULL COMMENT '保存时间',
  `update_time` varchar(30)  DEFAULT NULL COMMENT '最近更新时间',
  `is_active` int DEFAULT NULL COMMENT '是否启用',
  `width` int DEFAULT NULL COMMENT '点阵宽',
  `height` int DEFAULT NULL COMMENT '点阵高',
  `font` varchar(100)  DEFAULT NULL COMMENT '字体',
  `font_color` varchar(100)  DEFAULT NULL COMMENT '颜色',
  `font_size` varchar(500)  DEFAULT NULL COMMENT '字体大小',
  `font_horizontal_format` varchar(100)  DEFAULT NULL COMMENT '左右对齐',
  `font_vertical_format` varchar(100)  DEFAULT NULL COMMENT '上下对齐',
  `cms_type` int DEFAULT NULL COMMENT '情报板类型',
  `protocol` varchar(100)  DEFAULT NULL COMMENT '控制协议',
  `ip` varchar(100)  DEFAULT NULL COMMENT 'IP地址',
  `port` int DEFAULT NULL COMMENT '端口',
  `com_port` int DEFAULT NULL COMMENT '串口号',
  `com_baud` int DEFAULT NULL COMMENT '波特率',
  `verification` int DEFAULT NULL COMMENT '校验',
  `encryption_type` int DEFAULT NULL COMMENT '加解密方式',
  `protocol_flag` int DEFAULT NULL COMMENT '通信方式',
  `real_id` varchar(100)  DEFAULT NULL COMMENT '地址码',
  `tamper_resistant_ip` varchar(100)  DEFAULT NULL COMMENT '防篡改终端IP',
  `tamper_resistant_port` int DEFAULT NULL COMMENT '防篡改终端端口',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`device_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运情报板表，用于记录情报板基础信息（设备编号、设备名称、路段号、国家高速编号、方向编码、设施编码、桩号、经纬度）、显示配置（点阵尺寸、字体、颜色、对齐方式）、通信参数（控制协议、IP地址、端口、串口号、波特率、校验方式、加解密方式、通信方式、地址码）及安全配置（防篡改终端IP与端口），支持按设备编号、路段号、桩号、通信方式、控制协议等多维度查询，支持启用状态筛选及情报板数量统计:';
CREATE TABLE `ldly_base_d_co_visibility_detector` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备ID',
  `device_name` varchar(100)  DEFAULT NULL COMMENT '设备名称',
  `protocol_flag` int DEFAULT NULL COMMENT '通讯方式',
  `protocol` varchar(50)  DEFAULT NULL COMMENT '控制协议',
  `switch_ratio` double(5,2) DEFAULT NULL COMMENT '量程转换系数',
  `plc_id` varchar(32)  DEFAULT NULL COMMENT '所属PLC',
  `plc_prefix` varchar(100)  DEFAULT NULL COMMENT 'PLC标签',
  `co_data_tag` varchar(100)  DEFAULT NULL COMMENT 'CO数据标签',
  `vi_data_tag` varchar(100)  DEFAULT NULL COMMENT 'VI数据标签',
  `co_status_tag` varchar(100)  DEFAULT NULL COMMENT 'CO状态标签',
  `vi_status_tag` varchar(100)  DEFAULT NULL COMMENT 'VI状态标签',
  `update_rate` int DEFAULT NULL COMMENT '更新周期',
  `io_id` varchar(32)  DEFAULT NULL COMMENT '主控板',
  `road_no` varchar(32)  DEFAULT NULL COMMENT '路段号',
  `national_road_no` int DEFAULT NULL COMMENT '国家高速编号',
  `station_no` varchar(32)  DEFAULT NULL COMMENT '设施编码',
  `direction_no` int DEFAULT NULL COMMENT '方向编码',
  `mile_post` varchar(50)  DEFAULT NULL COMMENT '桩号',
  `mp_value` bigint DEFAULT NULL COMMENT '桩号数值',
  `x` bigint DEFAULT NULL COMMENT '经度',
  `y` bigint DEFAULT NULL COMMENT '纬度',
  `is_active` int DEFAULT NULL COMMENT '是否启用',
  `is_approving` int DEFAULT NULL COMMENT '是否审批中',
  `is_delete` int DEFAULT NULL COMMENT '是否删除',
  `create_time` varchar(30)  DEFAULT NULL COMMENT '保存时间',
  `update_time` varchar(30)  DEFAULT NULL COMMENT '最近更新时间',
  `co_data_address` varchar(100)  DEFAULT NULL COMMENT 'CO检测值点位',
  `co_status_address` varchar(100)  DEFAULT NULL COMMENT 'CO状态点位',
  `vi_data_address` varchar(100)  DEFAULT NULL COMMENT 'VI检测值点位',
  `vi_status_address` varchar(100)  DEFAULT NULL COMMENT 'VI状态点位',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`device_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运一氧化碳能见度检测器表，用于记录检测器基础信息（设备编号、设备名称、通讯方式、控制协议、量程转换系数）、关联信息（所属PLC、PLC标签、CO/VI数据与状态标签）、部署信息（更新周期、主控板、路段号、方向编码、桩号、经纬度）及点位信息（CO/VI检测值与状态点位），支持按设备名称、PLC编号、路段号、方向、协议类型等多维度查询，支持统计检测器总数、启用状态、协议分布及所属PLC分布，适用于平安隧道等场景的详细配置与运行状态管理:';
CREATE TABLE `ldly_base_d_decoder` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备ID',
  `device_name` varchar(100)  DEFAULT NULL COMMENT '设备名称',
  `ip` varchar(100)  DEFAULT NULL COMMENT 'IP地址',
  `port` int DEFAULT NULL COMMENT '端口',
  `user_name` varchar(100)  DEFAULT NULL COMMENT '用户名',
  `password` varchar(100)  DEFAULT NULL COMMENT '密码',
  `decoder_type` varchar(50)  DEFAULT NULL COMMENT '解码器类型',
  `protocol` varchar(50)  DEFAULT NULL COMMENT '解码协议',
  `road_no` varchar(32)  DEFAULT NULL COMMENT '路段号',
  `national_road_no` int DEFAULT NULL COMMENT '国家高速编号',
  `is_active` int DEFAULT NULL COMMENT '是否启用',
  `is_approving` int DEFAULT NULL COMMENT '是否审批中',
  `is_delete` int DEFAULT NULL COMMENT '是否删除',
  `create_time` varchar(30)  DEFAULT NULL COMMENT '保存时间',
  `update_time` varchar(30)  DEFAULT NULL COMMENT '最近更新时间',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`device_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运解码器信息表，用于记录解码器的设备编号、设备名称、IP地址、端口、用户名、类型、解码协议、路段号、国家高速编号及启用状态等详细信息，支持按设备编号、设备名称、IP地址、端口、用户名、类型、解码协议、路段号等多条件查询，可统计解码器总数、已启用数量及按类型分类统计，支持查询高分服务器相关设备及最新创建或更新的解码器:';
CREATE TABLE `ldly_base_d_draught_fan` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备ID',
  `device_name` varchar(100)  DEFAULT NULL COMMENT '设备名称',
  `update_rate` int DEFAULT NULL COMMENT '更新周期',
  `protocol_flag` int DEFAULT NULL COMMENT '通讯方式',
  `protocol` varchar(50)  DEFAULT NULL COMMENT '控制协议',
  `ip` varchar(100)  DEFAULT NULL COMMENT 'IP地址',
  `port` int DEFAULT NULL COMMENT '端口',
  `plc_id` varchar(32)  DEFAULT NULL COMMENT '所属PLC',
  `plc_prefix` varchar(100)  DEFAULT NULL COMMENT 'PLC标签',
  `control_tag` varchar(100)  DEFAULT NULL COMMENT '控制标签',
  `status_tag` varchar(100)  DEFAULT NULL COMMENT '状态标签',
  `error_back_tag` varchar(100)  DEFAULT NULL COMMENT '故障复位标签',
  `auto_status_tag` varchar(100)  DEFAULT NULL COMMENT '自动状态标签',
  `tunnel_id` int DEFAULT NULL COMMENT '隧道号',
  `fan_no` int DEFAULT NULL COMMENT '风机号',
  `com_port` varchar(50)  DEFAULT NULL COMMENT '串口号',
  `baud` int DEFAULT NULL COMMENT '波特率',
  `blast_volume` bigint DEFAULT NULL COMMENT '供风量',
  `io_id` varchar(32)  DEFAULT NULL COMMENT '主控板',
  `road_no` varchar(32)  DEFAULT NULL COMMENT '路段号',
  `national_road_no` int DEFAULT NULL COMMENT '国家高速编号',
  `station_no` varchar(32)  DEFAULT NULL COMMENT '设施编码',
  `direction_no` int DEFAULT NULL COMMENT '方向编码',
  `mile_post` varchar(50)  DEFAULT NULL COMMENT '桩号',
  `mp_value` bigint DEFAULT NULL COMMENT '桩号数值',
  `x` bigint DEFAULT NULL COMMENT '经度',
  `y` bigint DEFAULT NULL COMMENT '纬度',
  `is_active` int DEFAULT NULL COMMENT '是否启用',
  `is_approving` int DEFAULT NULL COMMENT '是否审批中',
  `is_delete` int DEFAULT NULL COMMENT '是否删除',
  `create_time` varchar(30)  DEFAULT NULL COMMENT '保存时间',
  `update_time` varchar(30)  DEFAULT NULL COMMENT '最近更新时间',
  `type` int DEFAULT NULL COMMENT '类型',
  `write_address` varchar(100)  DEFAULT NULL COMMENT '控制点位',
  `read_address` varchar(100)  DEFAULT NULL COMMENT '运行状态点位',
  `auto_status_address` varchar(100)  DEFAULT NULL COMMENT '自动状态点位',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`device_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运风机表，用于记录风机设备编号、名称、更新周期、通讯方式、控制协议、IP地址、端口、所属PLC、PLC标签、控制标签、状态标签、故障复位标签、自动状态标签、隧道号、风机号、串口号、波特率、供风量、主控板、路段号、方向编码、桩号、经纬度、类型、控制点位、运行状态点位、自动状态点位等详细信息，支持按设备名称、隧道号、风机号、PLC编号、路段号、供风量等条件查询风机信息，支持查询射流风机、轴流风机、已启用风机、特定位置风机（如平安往博罗）及其控制配置和位置信息，支持统计风机总数量、已启用数量、按类型、隧道号、PLC编号统计数量及供风量，支持多维度设备信息查询与状态分析:';
CREATE TABLE `ldly_base_d_dvr` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备ID',
  `device_name` varchar(100)  DEFAULT NULL COMMENT '设备名称',
  `road_no` varchar(32)  DEFAULT NULL COMMENT '路段号',
  `national_road_no` int DEFAULT NULL COMMENT '国家高速编号',
  `station_no` varchar(32)  DEFAULT NULL COMMENT '设施编码',
  `dvr_type` int DEFAULT NULL COMMENT '类型',
  `ip` varchar(100)  DEFAULT NULL COMMENT 'IP地址',
  `port` int DEFAULT NULL COMMENT '端口',
  `user_name` varchar(100)  DEFAULT NULL COMMENT '用户名',
  `password` varchar(100)  DEFAULT NULL COMMENT '密码',
  `dvr_protocol` varchar(100)  DEFAULT NULL COMMENT '存储协议',
  `manufacturer` int DEFAULT NULL COMMENT '厂家',
  `is_active` int DEFAULT NULL COMMENT '是否启用',
  `is_approving` int DEFAULT NULL COMMENT '是否审批中',
  `is_delete` int DEFAULT NULL COMMENT '是否删除',
  `create_time` varchar(30)  DEFAULT NULL COMMENT '保存时间',
  `update_time` varchar(30)  DEFAULT NULL COMMENT '最近更新时间',
  `is_special` int DEFAULT NULL COMMENT '通道号是否需要特殊处理',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`device_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运录像机表，用于记录录像机的基础信息、网络配置、存储协议及运行状态，支持按设备编号、设备名称、路段号、国家高速编号、设施编码、类型、IP地址、端口、厂家等多维度查询，可统计录像机总数、已启用数量及按类型、厂家、路段号分类统计，支持查询特殊处理设备、NVR32设备、特定IP或协议设备，以及最新创建或更新的录像机信息:';
CREATE TABLE `ldly_base_d_emergency_phone` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备ID',
  `device_name` varchar(100)  DEFAULT NULL COMMENT '设备名称',
  `road_no` varchar(32)  DEFAULT NULL COMMENT '路段号',
  `national_road_no` int DEFAULT NULL COMMENT '国家高速编号',
  `station_no` varchar(32)  DEFAULT NULL COMMENT '设施编码',
  `direction_no` int DEFAULT NULL COMMENT '方向编码',
  `mile_post` varchar(50)  DEFAULT NULL COMMENT '桩号',
  `mp_value` bigint DEFAULT NULL COMMENT '桩号数值',
  `x` bigint DEFAULT NULL COMMENT '经度',
  `y` bigint DEFAULT NULL COMMENT '纬度',
  `address` varchar(200)  DEFAULT NULL COMMENT '设备地址码',
  `alarm_host_id` varchar(32)  DEFAULT NULL COMMENT '所属报警主机',
  `is_active` int DEFAULT NULL COMMENT '是否启用',
  `is_approving` int DEFAULT NULL COMMENT '是否审批中',
  `is_delete` int DEFAULT NULL COMMENT '是否删除',
  `create_time` varchar(30)  DEFAULT NULL COMMENT '保存时间',
  `update_time` varchar(30)  DEFAULT NULL COMMENT '最近更新时间',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`device_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运紧急电话表，用于记录紧急电话设备的基础信息、位置信息、运行状态及与报警主机的关联关系，支持按设备编号、设备名称、路段号、国家高速编号、设施编码、方向编码、桩号、经纬度、设备地址码、所属报警主机等多维度查询，支持统计紧急电话总数及按路段号、方向编码、报警主机分类统计数量，支持查询设备状态（启用/在线/离线/故障）及最新创建或更新的设备信息，支持模糊查询设备名称及关联报警主机详细信息:';
CREATE TABLE `ldly_base_d_environment_monitor` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备ID',
  `device_name` varchar(100)  DEFAULT NULL COMMENT '设备名称',
  `road_no` varchar(32)  DEFAULT NULL COMMENT '路段号',
  `national_road_no` int DEFAULT NULL COMMENT '国家高速编号',
  `station_no` varchar(32)  DEFAULT NULL COMMENT '设施编码',
  `direction_no` int DEFAULT NULL COMMENT '方向编码',
  `mile_post` varchar(50)  DEFAULT NULL COMMENT '桩号',
  `mp_value` bigint DEFAULT NULL COMMENT '桩号数值',
  `x` bigint DEFAULT NULL COMMENT '经度',
  `y` bigint DEFAULT NULL COMMENT '纬度',
  `address` varchar(200)  DEFAULT NULL COMMENT '设备地址码',
  `alarm_host_id` varchar(32)  DEFAULT NULL COMMENT '所属报警主机',
  `device_subtype_no` int DEFAULT NULL COMMENT '设备子类型',
  `link_device_id` varchar(32)  DEFAULT NULL COMMENT '关联设备ID',
  `link_device_type` int DEFAULT NULL COMMENT '关联设备类型',
  `is_active` int DEFAULT NULL COMMENT '是否启用',
  `is_approving` int DEFAULT NULL COMMENT '是否审批中',
  `is_delete` int DEFAULT NULL COMMENT '是否删除',
  `create_time` varchar(30)  DEFAULT NULL COMMENT '保存时间',
  `update_time` varchar(30)  DEFAULT NULL COMMENT '最近更新时间',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`device_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运动环设备表，用于记录路运系统中各类动环设备的基础信息、位置信息及关联关系，包括设备编号、设备名称、路段号、国家高速编号、设施编码、方向编码、桩号、经纬度、设备地址码、所属报警主机、设备子类型、关联设备编号及类型等，支持按设备名称、路段号、报警主机、方向等多条件查询设备详细信息，支持统计设备总数、已启用设备数，并可按设备子类型或报警主机分类统计，适用于设备状态监控、运维管理和关联关系分析:';
CREATE TABLE `ldly_base_d_fire_alarm` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备ID',
  `device_name` varchar(100)  DEFAULT NULL COMMENT '设备名称',
  `road_no` varchar(32)  DEFAULT NULL COMMENT '路段号',
  `national_road_no` int DEFAULT NULL COMMENT '国家高速编号',
  `station_no` varchar(32)  DEFAULT NULL COMMENT '设施编码',
  `direction_no` int DEFAULT NULL COMMENT '方向编码',
  `mile_post` varchar(50)  DEFAULT NULL COMMENT '桩号',
  `mp_value` bigint DEFAULT NULL COMMENT '桩号数值',
  `x` bigint DEFAULT NULL COMMENT '经度',
  `y` bigint DEFAULT NULL COMMENT '纬度',
  `address` varchar(200)  DEFAULT NULL COMMENT '设备地址码',
  `alarm_host_id` varchar(32)  DEFAULT NULL COMMENT '所属报警主机',
  `fire_alarm_type` int DEFAULT NULL COMMENT '类型',
  `is_active` int DEFAULT NULL COMMENT '是否启用',
  `is_approving` int DEFAULT NULL COMMENT '是否审批中',
  `is_delete` int DEFAULT NULL COMMENT '是否删除',
  `create_time` varchar(30)  DEFAULT NULL COMMENT '保存时间',
  `update_time` varchar(30)  DEFAULT NULL COMMENT '最近更新时间',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`device_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运火灾报警器表，用于记录路运系统中火灾报警器的设备编号、设备名称、路段号、国家高速编号、设施编码、方向编码、桩号、经纬度、设备地址码、所属报警主机及类型等基础与位置信息，支持按多字段精确或模糊查询，支持统计总数量、启用状态、设备类型、路段分布、报警主机分布及在线/离线/故障状态，可查询最新创建或更新的设备及其关联信息，支持按桩号范围、方向、设备名称关键词等条件进行复杂检索和分析:';
CREATE TABLE `ldly_base_d_fire_pool` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备ID',
  `device_name` varchar(100)  DEFAULT NULL COMMENT '设备名称',
  `area` bigint DEFAULT NULL COMMENT '底面积',
  `update_rate` int DEFAULT NULL COMMENT '更新周期',
  `protocol` varchar(50)  DEFAULT NULL COMMENT '控制协议',
  `protocol_flag` int DEFAULT NULL COMMENT '通讯方式',
  `plc_id` varchar(32)  DEFAULT NULL COMMENT '所属PLC',
  `plc_prefix` varchar(100)  DEFAULT NULL COMMENT 'PLC标签',
  `stage_tag` varchar(100)  DEFAULT NULL COMMENT '水位标签',
  `high_stage_tag` varchar(100)  DEFAULT NULL COMMENT '高水位标签',
  `middle_stage_tag` varchar(100)  DEFAULT NULL COMMENT '中水位标签',
  `low_stage_tag` varchar(100)  DEFAULT NULL COMMENT '低水位标签',
  `com_port` varchar(100)  DEFAULT NULL COMMENT '串口号',
  `baud` int DEFAULT NULL COMMENT '波特率',
  `ip` varchar(100)  DEFAULT NULL COMMENT 'IP地址',
  `port` int DEFAULT NULL COMMENT '端口',
  `address` varchar(100)  DEFAULT NULL COMMENT '地址码',
  `water_level_type` int DEFAULT NULL COMMENT '水位数值类型',
  `io_id` varchar(32)  DEFAULT NULL COMMENT '主控板id',
  `road_no` varchar(32)  DEFAULT NULL COMMENT '路段号',
  `national_road_no` int DEFAULT NULL COMMENT '国家高速编号',
  `station_no` varchar(32)  DEFAULT NULL COMMENT '设施编码',
  `direction_no` int DEFAULT NULL COMMENT '方向编码',
  `mile_post` varchar(50)  DEFAULT NULL COMMENT '桩号',
  `mp_value` bigint DEFAULT NULL COMMENT '桩号数值',
  `x` bigint DEFAULT NULL COMMENT '经度',
  `y` bigint DEFAULT NULL COMMENT '纬度',
  `is_active` int DEFAULT NULL COMMENT '是否启用',
  `is_approving` int DEFAULT NULL COMMENT '是否审批中',
  `is_delete` int DEFAULT NULL COMMENT '是否删除',
  `create_time` varchar(30)  DEFAULT NULL COMMENT '保存时间',
  `update_time` varchar(30)  DEFAULT NULL COMMENT '最近更新时间',
  `read_address` varchar(100)  DEFAULT NULL COMMENT '水池液位点位',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`device_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运消防池表，用于记录消防池基础信息、设备参数、通信配置、位置信息及运行状态，支持按设备编号、设备名称、所属PLC、水位数值类型、控制协议、设施编码、方向编码、路段号、桩号、经纬度等多维度查询，支持查询平安隧道相关消防池及其水位标签配置，支持统计消防池总数、启用状态、分类数量及协议使用情况，支持按设施编码、PLC编号、水位类型等进行关联分析和统计:';
CREATE TABLE `ldly_base_d_fire_pump` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备ID',
  `device_name` varchar(100)  DEFAULT NULL COMMENT '设备名称',
  `protocol_flag` int DEFAULT NULL COMMENT '通讯方式',
  `protocol` varchar(50)  DEFAULT NULL COMMENT '控制协议',
  `update_rate` int DEFAULT NULL COMMENT '更新周期',
  `plc_id` varchar(32)  DEFAULT NULL COMMENT '所属PLC',
  `plc_prefix` varchar(100)  DEFAULT NULL COMMENT 'PLC标签',
  `control_tag` varchar(100)  DEFAULT NULL COMMENT '控制标签',
  `status_tag` varchar(100)  DEFAULT NULL COMMENT '运行状态标签',
  `auto_status_tag` varchar(100)  DEFAULT NULL COMMENT '自动状态标签',
  `io_id` varchar(32)  DEFAULT NULL COMMENT '主控板',
  `road_no` varchar(32)  DEFAULT NULL COMMENT '路段号',
  `national_road_no` int DEFAULT NULL COMMENT '国家高速编号',
  `station_no` varchar(32)  DEFAULT NULL COMMENT '设施编码',
  `direction_no` int DEFAULT NULL COMMENT '方向编码',
  `mile_post` varchar(50)  DEFAULT NULL COMMENT '桩号',
  `mp_value` bigint DEFAULT NULL COMMENT '桩号数值',
  `x` bigint DEFAULT NULL COMMENT '经度',
  `y` bigint DEFAULT NULL COMMENT '纬度',
  `is_active` int DEFAULT NULL COMMENT '是否启用',
  `is_approving` int DEFAULT NULL COMMENT '是否审批中',
  `is_delete` int DEFAULT NULL COMMENT '是否删除',
  `create_time` varchar(30)  DEFAULT NULL COMMENT '保存时间',
  `update_time` varchar(30)  DEFAULT NULL COMMENT '最近更新时间',
  `write_address` varchar(100)  DEFAULT NULL COMMENT '控制点位',
  `run_status_address` varchar(100)  DEFAULT NULL COMMENT '运行状态点位',
  `status_address` varchar(100)  DEFAULT NULL COMMENT '故障状态点位',
  `stop_status_address` varchar(100)  DEFAULT NULL COMMENT '停止状态位点位',
  `auto_status_address` varchar(100)  DEFAULT NULL COMMENT '自动状态点位',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`device_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运消防泵表，用于记录消防泵的设备编号、名称、通讯方式、控制协议、更新周期、所属PLC、PLC标签、控制标签、运行状态标签、自动状态标签、主控板、路段号、方向编码、桩号、经纬度、控制点位、运行状态点位、故障状态点位、停止状态点位、自动状态点位等详细信息，支持按设备编号、名称、PLC、路段号、设施编码、方向编码、协议类型等多维度查询，可统计消防泵总数、已启用数量及按所属PLC、协议类型、设施编码分类统计，支持查询平安隧道相关设备及其标签和点位配置，便于设备管理、运行监控与协议分析:';
CREATE TABLE `ldly_base_d_lane_pilot_lamp` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备ID',
  `device_name` varchar(100)  DEFAULT NULL COMMENT '设备名称',
  `update_rate` int DEFAULT NULL COMMENT '更新周期',
  `plc_id` varchar(32)  DEFAULT NULL COMMENT '所属PLC',
  `plc_prefix` varchar(100)  DEFAULT NULL COMMENT 'PLC标签',
  `protocol_flag` int DEFAULT NULL COMMENT '通讯方式',
  `protocol` varchar(50)  DEFAULT NULL COMMENT '控制协议',
  `lane_no` int DEFAULT NULL COMMENT '车道号',
  `control_tag` varchar(100)  DEFAULT NULL COMMENT '控制标签',
  `front_status_tag` varchar(100)  DEFAULT NULL COMMENT '正面状态标签',
  `verso_status_tag` varchar(100)  DEFAULT NULL COMMENT '反面状态标签',
  `com_port` varchar(100)  DEFAULT NULL COMMENT '串口号',
  `baud` int DEFAULT NULL COMMENT '波特率',
  `io_id` varchar(32)  DEFAULT NULL COMMENT '主控板',
  `ip` varchar(100)  DEFAULT NULL COMMENT 'ip',
  `port` int DEFAULT NULL COMMENT '端口',
  `road_no` varchar(32)  DEFAULT NULL COMMENT '路段号',
  `national_road_no` int DEFAULT NULL COMMENT '国家高速编号',
  `station_no` varchar(32)  DEFAULT NULL COMMENT '设施编码',
  `direction_no` int DEFAULT NULL COMMENT '方向编码',
  `mile_post` varchar(50)  DEFAULT NULL COMMENT '桩号',
  `mp_value` bigint DEFAULT NULL COMMENT '桩号数值',
  `x` bigint DEFAULT NULL COMMENT '经度',
  `y` bigint DEFAULT NULL COMMENT '纬度',
  `is_active` int DEFAULT NULL COMMENT '是否启用',
  `is_approving` int DEFAULT NULL COMMENT '是否审批中',
  `is_delete` int DEFAULT NULL COMMENT '是否删除',
  `create_time` varchar(30)  DEFAULT NULL COMMENT '保存时间',
  `update_time` varchar(30)  DEFAULT NULL COMMENT '最近更新时间',
  `write_address` varchar(100)  DEFAULT NULL COMMENT '控制点位',
  `read_address` varchar(100)  DEFAULT NULL COMMENT '车指反馈点位',
  `f_status_address` varchar(100)  DEFAULT NULL COMMENT '正面反馈点位',
  `v_status_address` varchar(100)  DEFAULT NULL COMMENT '反面反馈点位',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`device_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运车道指示灯表，用于记录车道指示灯的设备编号、设备名称、所属PLC、PLC标签、通讯方式、控制协议、车道号、控制标签、状态标签、串口号、波特率、主控板、IP地址、端口、路段号、方向编码、桩号、经纬度、控制点位、反馈点位等详细信息，支持按设备名称、PLC编号、车道号、路段号、设施编码、方向、协议类型等多维度查询和统计，可统计总数量、已启用数量，按车道号、PLC、设施编码等分类统计，支持查询最新创建或更新的设备信息，支持查询设备的正面和反面状态标签、反馈点位，支持按路段、方向、协议类型、设施编码等条件筛选设备信息:';
CREATE TABLE `ldly_base_d_light_detector` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备ID',
  `device_name` varchar(100)  DEFAULT NULL COMMENT '设备名称',
  `protocol_flag` int DEFAULT NULL COMMENT '通讯方式',
  `protocol` varchar(50)  DEFAULT NULL COMMENT '控制协议',
  `switch_ratio` double(5,2) DEFAULT NULL COMMENT '量程转换系数',
  `update_rate` int DEFAULT NULL COMMENT '更新周期',
  `plc_id` varchar(32)  DEFAULT NULL COMMENT '所属PLC',
  `plc_prefix` varchar(100)  DEFAULT NULL COMMENT 'PLC标签',
  `li_data_tag` varchar(100)  DEFAULT NULL COMMENT 'LI数据标签',
  `lo_data_tag` varchar(100)  DEFAULT NULL COMMENT 'LO数据标签',
  `li_status_tag` varchar(100)  DEFAULT NULL COMMENT 'LI状态标签',
  `lo_status_tag` varchar(100)  DEFAULT NULL COMMENT 'LO状态标签',
  `data_type` int DEFAULT NULL COMMENT '数据类型',
  `io_id` varchar(32)  DEFAULT NULL COMMENT '主控板',
  `road_no` varchar(32)  DEFAULT NULL COMMENT '路段号',
  `national_road_no` int DEFAULT NULL COMMENT '国家高速编号',
  `station_no` varchar(32)  DEFAULT NULL COMMENT '设施编码',
  `direction_no` int DEFAULT NULL COMMENT '方向编码',
  `mile_post` varchar(50)  DEFAULT NULL COMMENT '桩号',
  `mp_value` bigint DEFAULT NULL COMMENT '桩号数值',
  `x` bigint DEFAULT NULL COMMENT '经度',
  `y` bigint DEFAULT NULL COMMENT '纬度',
  `is_active` int DEFAULT NULL COMMENT '是否启用',
  `is_approving` int DEFAULT NULL COMMENT '是否审批中',
  `is_delete` int DEFAULT NULL COMMENT '是否删除',
  `create_time` varchar(30)  DEFAULT NULL COMMENT '保存时间',
  `update_time` varchar(30)  DEFAULT NULL COMMENT '最近更新时间',
  `li_data_address` varchar(100)  DEFAULT NULL COMMENT 'LI检测值点位',
  `li_status_address` varchar(100)  DEFAULT NULL COMMENT 'LI状态点位',
  `lo_data_address` varchar(100)  DEFAULT NULL COMMENT 'LO检测值点位',
  `lo_status_address` varchar(100)  DEFAULT NULL COMMENT 'LO状态点位',
  `ip` varchar(100)  DEFAULT NULL COMMENT 'IP地址',
  `port` int DEFAULT NULL COMMENT '端口',
  PRIMARY KEY (`commonId`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运光强检测器表，用于记录光强检测设备的基础信息、通信方式、控制协议、数据标签、位置信息及运行状态，支持按设备编号、设备名称、所属PLC、数据类型、方向编码、设施编码、协议类型等多维度查询，可统计光强检测器总数、启用状态、协议分布及设施编码分布，支持查询平安隧道相关设备及其详细配置、点位、标签信息，支持按路段号、桩号、经纬度、IP地址、端口等参数进行精细化检索和分析:';
CREATE TABLE `ldly_base_d_lighting` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备ID',
  `device_name` varchar(100)  DEFAULT NULL COMMENT '设备名称',
  `update_rate` int DEFAULT NULL COMMENT '更新周期',
  `tunnel_no` int DEFAULT NULL COMMENT '隧道号',
  `loop_no` int DEFAULT NULL COMMENT '回路号',
  `protocol_flag` int DEFAULT NULL COMMENT '通讯方式',
  `protocol` varchar(50)  DEFAULT NULL COMMENT '控制协议',
  `plc_id` varchar(32)  DEFAULT NULL COMMENT '所属PLC',
  `plc_prefix` varchar(100)  DEFAULT NULL COMMENT 'PLC标签',
  `ip` varchar(100)  DEFAULT NULL COMMENT 'IP地址',
  `port` int DEFAULT NULL COMMENT '端口',
  `control_tag` varchar(100)  DEFAULT NULL COMMENT '控制标签',
  `status_tag` varchar(100)  DEFAULT NULL COMMENT '状态标签',
  `auto_status_tag` varchar(100)  DEFAULT NULL COMMENT '自动状态标签',
  `time_control_tag` varchar(100)  DEFAULT NULL COMMENT '时控标签',
  `status_address` varchar(100)  DEFAULT NULL COMMENT '状态地址',
  `control_address` varchar(100)  DEFAULT NULL COMMENT '控制地址',
  `auto_status_address` varchar(100)  DEFAULT NULL COMMENT '自动状态地址',
  `io_id` varchar(32)  DEFAULT NULL COMMENT '主控板',
  `road_no` varchar(32)  DEFAULT NULL COMMENT '路段号',
  `national_road_no` int DEFAULT NULL COMMENT '国家高速编号',
  `station_no` varchar(32)  DEFAULT NULL COMMENT '设施编码',
  `direction_no` int DEFAULT NULL COMMENT '方向编码',
  `mile_post` varchar(50)  DEFAULT NULL COMMENT '桩号',
  `mp_value` bigint DEFAULT NULL COMMENT '桩号数值',
  `x` bigint DEFAULT NULL COMMENT '经度',
  `y` bigint DEFAULT NULL COMMENT '纬度',
  `is_active` int DEFAULT NULL COMMENT '是否启用',
  `is_approving` int DEFAULT NULL COMMENT '是否审批中',
  `is_delete` int DEFAULT NULL COMMENT '是否删除',
  `create_time` varchar(30)  DEFAULT NULL COMMENT '保存时间',
  `update_time` varchar(30)  DEFAULT NULL COMMENT '最近更新时间',
  `write_address` varchar(100)  DEFAULT NULL COMMENT '控制点位',
  `read_address` varchar(100)  DEFAULT NULL COMMENT '开关状态反馈点位',
  `lighting_type` int DEFAULT NULL COMMENT '设备类型（照明类型）',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`device_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运照明表，用于记录路运系统中所有照明设备的基础信息、控制配置、通信参数及运行状态，包括设备编号、设备名称、更新周期、隧道号、回路号、通讯方式、控制协议、所属PLC、PLC标签、IP地址、端口、控制标签、状态标签、自动状态标签、时控标签、状态地址、控制地址、自动状态地址、主控板、路段号、方向编码、桩号、经纬度、控制点位、开关状态反馈点位、照明类型等，支持按设备名称、隧道号、回路号、所属PLC、设施编码、协议类型等多维度查询，支持统计照明总数量、已启用数量、按类型、隧道号、所属PLC、设施编码分类统计，支持查询最新创建或更新的照明设备及其关联PLC信息:';
CREATE TABLE `ldly_base_d_plc` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备ID',
  `device_name` varchar(100)  DEFAULT NULL COMMENT '设备名称',
  `road_no` varchar(32)  DEFAULT NULL COMMENT '路段号',
  `national_road_no` int DEFAULT NULL COMMENT '国家高速编号',
  `station_no` varchar(32)  DEFAULT NULL COMMENT '设施编码',
  `mile_post` varchar(50)  DEFAULT NULL COMMENT '桩号',
  `direction_no` int DEFAULT NULL COMMENT '方向编码',
  `x` bigint DEFAULT NULL COMMENT '经度',
  `y` bigint DEFAULT NULL COMMENT '纬度',
  `is_active` int DEFAULT NULL COMMENT '是否启用',
  `is_approving` int DEFAULT NULL COMMENT '是否审批中',
  `is_delete` int DEFAULT NULL COMMENT '是否删除',
  `create_time` varchar(30)  DEFAULT NULL COMMENT '保存时间',
  `update_time` varchar(30)  DEFAULT NULL COMMENT '最近更新时间',
  `is_reserve` int DEFAULT NULL COMMENT '是否双冗余',
  `ip` varchar(100)  DEFAULT NULL COMMENT '首选IP地址',
  `ip2` varchar(100)  DEFAULT NULL COMMENT '备用IP地址',
  `manufacturer` int DEFAULT NULL COMMENT '厂家',
  `plc_prefix` varchar(100)  DEFAULT NULL COMMENT 'PLC前缀',
  `plc_prefix2` varchar(100)  DEFAULT NULL COMMENT 'PLC前缀',
  `status_tag` varchar(100)  DEFAULT NULL COMMENT 'PLC状态标签',
  `ip_tag` varchar(100)  DEFAULT NULL COMMENT 'IP标签',
  `plc_time_tag` varchar(100)  DEFAULT NULL COMMENT 'PLC时间标签',
  `main_status_tag` varchar(100)  DEFAULT NULL COMMENT '主备状态标签',
  `time1_tag` varchar(100)  DEFAULT NULL COMMENT '时控1时间段标签',
  `enb_time1_tag` varchar(100)  DEFAULT NULL COMMENT '时控1使能标签',
  `time2_tag` varchar(100)  DEFAULT NULL COMMENT '时控2时间段标签',
  `enb_time2_tag` varchar(100)  DEFAULT NULL COMMENT '时控2使能标签',
  `time3_tag` varchar(100)  DEFAULT NULL COMMENT '时控3时间段标签',
  `enb_time3_tag` varchar(100)  DEFAULT NULL COMMENT '时控3使能标签',
  `time4_tag` varchar(100)  DEFAULT NULL COMMENT '时控4时间段标签',
  `enb_time4_tag` varchar(100)  DEFAULT NULL COMMENT '时控4使能标签',
  `time5_tag` varchar(100)  DEFAULT NULL COMMENT '时控5时间段标签',
  `enb_time5_tag` varchar(100)  DEFAULT NULL COMMENT '时控5使能标签',
  `time6_tag` varchar(100)  DEFAULT NULL COMMENT '时控6时间段标签',
  `enb_time6_tag` varchar(100)  DEFAULT NULL COMMENT '时控6使能标签',
  `update_rate` int DEFAULT NULL COMMENT '更新周期',
  `protocol` varchar(50)  DEFAULT NULL COMMENT '控制协议',
  `mp_value` bigint DEFAULT NULL COMMENT '桩号数值',
  `read_address` varchar(100)  DEFAULT NULL COMMENT '运行状态点位',
  `port` int DEFAULT NULL COMMENT '端口',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`device_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运PLC表，用于记录路运系统中所有PLC设备的基础信息、网络配置、运行状态及时控配置，支持按设备编号、设备名称、路段号、设施编码、桩号、方向编码、经纬度、厂家、控制协议等多维度查询，支持查询双冗余状态、主备状态、IP地址、标签配置、时控配置及运行状态点位，支持统计PLC总数、已启用数量、按厂家或路段号分类统计，可关联查询风机、照明、车道指示灯、交通信号灯、消防泵、消防池、卷帘门等设施及其所属PLC的详细信息:';
CREATE TABLE `ldly_base_d_rolling_door` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备ID',
  `device_name` varchar(100)  DEFAULT NULL COMMENT '设备名称',
  `protocol_flag` int DEFAULT NULL COMMENT '通讯方式',
  `protocol` varchar(50)  DEFAULT NULL COMMENT '控制协议',
  `update_rate` int DEFAULT NULL COMMENT '更新周期',
  `plc_id` varchar(32)  DEFAULT NULL COMMENT '所属PLC',
  `plc_prefix` varchar(100)  DEFAULT NULL COMMENT 'PLC标签',
  `control_tag` varchar(100)  DEFAULT NULL COMMENT '控制标签',
  `up_status_tag` varchar(100)  DEFAULT NULL COMMENT '上升状态标签',
  `down_status_tag` varchar(100)  DEFAULT NULL COMMENT '下降状态标签',
  `run_status_tag` varchar(100)  DEFAULT NULL COMMENT '运行状态标签',
  `io_id` varchar(32)  DEFAULT NULL COMMENT '主控板',
  `road_no` varchar(32)  DEFAULT NULL COMMENT '路段号',
  `national_road_no` int DEFAULT NULL COMMENT '国家高速编号',
  `station_no` varchar(32)  DEFAULT NULL COMMENT '设施编码',
  `direction_no` int DEFAULT NULL COMMENT '方向编码',
  `mile_post` varchar(50)  DEFAULT NULL COMMENT '桩号',
  `mp_value` bigint DEFAULT NULL COMMENT '桩号数值',
  `x` bigint DEFAULT NULL COMMENT '经度',
  `y` bigint DEFAULT NULL COMMENT '纬度',
  `status_address` varchar(100)  DEFAULT NULL COMMENT '状态地址',
  `control_address` varchar(100)  DEFAULT NULL COMMENT '控制地址',
  `is_active` int DEFAULT NULL COMMENT '是否启用',
  `is_approving` int DEFAULT NULL COMMENT '是否审批中',
  `is_delete` int DEFAULT NULL COMMENT '是否删除',
  `create_time` varchar(30)  DEFAULT NULL COMMENT '保存时间',
  `update_time` varchar(30)  DEFAULT NULL COMMENT '最近更新时间',
  `write_address` varchar(100)  DEFAULT NULL COMMENT '控制点位',
  `read_address` varchar(100)  DEFAULT NULL COMMENT '状态点位',
  `up_status_address` varchar(100)  DEFAULT NULL COMMENT '上限状态点位',
  `down_status_address` varchar(100)  DEFAULT NULL COMMENT '下限状态点位',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`device_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运卷帘门表，用于记录卷帘门设备基础信息、通讯控制参数、运行状态及位置信息，支持按设备编号、设备名称、所属PLC、路段号、方向编码、设施编码、控制协议等多维度查询，可统计卷帘门总数、启用状态、在线/离线/故障数量，支持平安隧道相关卷帘门的详细信息及标签、点位、协议配置查询，适用于路运系统设备管理、运行监控与数据分析:';
CREATE TABLE `ldly_base_d_splicing_controller` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备ID',
  `device_name` varchar(100)  DEFAULT NULL COMMENT '设备名称',
  `sc_type` int DEFAULT NULL COMMENT '拼控器类型',
  `protocol_flag` int DEFAULT NULL COMMENT '通讯方式',
  `ip` varchar(100)  DEFAULT NULL COMMENT 'IP地址',
  `port` int DEFAULT NULL COMMENT '端口',
  `com_port` int DEFAULT NULL COMMENT '串口号',
  `com_baud` int DEFAULT NULL COMMENT '波特率',
  `user_name` varchar(100)  DEFAULT NULL COMMENT '用户名',
  `password` varchar(100)  DEFAULT NULL COMMENT '密码',
  `protocol` varchar(50)  DEFAULT NULL COMMENT '厂家协议',
  `road_no` varchar(32)  DEFAULT NULL COMMENT '路段号',
  `national_road_no` int DEFAULT NULL COMMENT '国家高速编号',
  `is_active` int DEFAULT NULL COMMENT '是否启用',
  `is_approving` int DEFAULT NULL COMMENT '是否审批中',
  `is_delete` int DEFAULT NULL COMMENT '是否删除',
  `create_time` varchar(30)  DEFAULT NULL COMMENT '保存时间',
  `update_time` varchar(30)  DEFAULT NULL COMMENT '最近更新时间',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`device_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运拼控器信息表，用于记录拼控器的设备编号、设备名称、类型、通讯方式、IP地址、端口、串口号、波特率、用户名、厂家协议、路段号、国家高速编号等详细信息，支持查询所有拼控器的基础信息字段，按设备名称、类型、IP地址、路段号等条件精准筛选，支持查询已启用设备及特定类型（如纯拼接、解拼一体机）设备，可统计拼控器总数、已启用数量及按类型分类统计，满足多维度查询与统计分析需求:';
CREATE TABLE `ldly_base_d_traffic_signal_light` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备ID',
  `device_name` varchar(100)  DEFAULT NULL COMMENT '设备名称',
  `protocol_flag` int DEFAULT NULL COMMENT '通讯方式',
  `protocol` varchar(50)  DEFAULT NULL COMMENT '控制协议',
  `update_rate` int DEFAULT NULL COMMENT '更新周期',
  `plc_id` varchar(32)  DEFAULT NULL COMMENT '所属PLC',
  `plc_prefix` varchar(100)  DEFAULT NULL COMMENT 'PLC标签',
  `green_status_tag` varchar(100)  DEFAULT NULL COMMENT '绿灯状态标签',
  `red_status_tag` varchar(100)  DEFAULT NULL COMMENT '红灯状态标签',
  `yellow_status_tag` varchar(100)  DEFAULT NULL COMMENT '黄灯状态标签',
  `turned_status_tag` varchar(100)  DEFAULT NULL COMMENT '转向灯状态标签',
  `io_id` varchar(32)  DEFAULT NULL COMMENT '主控板',
  `road_no` varchar(32)  DEFAULT NULL COMMENT '路段号',
  `national_road_no` int DEFAULT NULL COMMENT '国家高速编号',
  `station_no` varchar(32)  DEFAULT NULL COMMENT '设施编码',
  `direction_no` int DEFAULT NULL COMMENT '方向编码',
  `mile_post` varchar(50)  DEFAULT NULL COMMENT '桩号',
  `mp_value` bigint DEFAULT NULL COMMENT '桩号数值',
  `x` bigint DEFAULT NULL COMMENT '经度',
  `y` bigint DEFAULT NULL COMMENT '纬度',
  `is_active` int DEFAULT NULL COMMENT '是否启用',
  `is_approving` int DEFAULT NULL COMMENT '是否审批中',
  `is_delete` int DEFAULT NULL COMMENT '是否删除',
  `create_time` varchar(30)  DEFAULT NULL COMMENT '保存时间',
  `update_time` varchar(30)  DEFAULT NULL COMMENT '最近更新时间',
  `control_tag` varchar(1000)  DEFAULT NULL COMMENT '控制标签',
  `write_address` varchar(100)  DEFAULT NULL COMMENT '控制点位',
  `read_address` varchar(100)  DEFAULT NULL COMMENT '运行状态点位',
  `red_status_address` varchar(100)  DEFAULT NULL COMMENT '红灯状态点位',
  `green_status_address` varchar(100)  DEFAULT NULL COMMENT '绿灯状态点位',
  `yellow_status_address` varchar(100)  DEFAULT NULL COMMENT '黄灯状态点位',
  `turn_status_address` varchar(100)  DEFAULT NULL COMMENT '转向灯状态点位',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`device_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运交通信号灯表，用于记录交通信号灯的设备编号、设备名称、通讯方式、控制协议、更新周期、所属PLC、PLC标签、各灯状态标签及点位、主控板、路段号、方向编码、桩号、经纬度、控制标签、控制点位、运行状态及各类灯状态点位信息，支持查询所有信号灯的设备编号、设备名称、通讯方式、控制协议、更新周期、所属PLC、PLC标签、各灯状态标签、主控板、路段号、方向编码、桩号、经纬度、控制标签、控制点位、运行状态点位及各类灯状态点位，支持查询已启用信号灯、平安隧道信号灯的详细信息及配置，支持按PLC、路段号、设施编码、协议类型、方向等多维度查询信号灯及其所属PLC信息，支持统计信号灯总数、已启用数量、按PLC、设施编码、协议类型分类统计数量，支持查询最新创建或更新的信号灯及各类设备总数统计:';
CREATE TABLE `ldly_base_d_vehicle_detector` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备ID',
  `device_name` varchar(100)  DEFAULT NULL COMMENT '设备名称',
  `protocol_flag` int DEFAULT NULL COMMENT '通讯方式',
  `protocol` varchar(50)  DEFAULT NULL COMMENT '控制协议',
  `ip` varchar(100)  DEFAULT NULL COMMENT 'IP地址',
  `port` int DEFAULT NULL COMMENT '端口',
  `com_port` int DEFAULT NULL COMMENT '串口号',
  `com_baud` int DEFAULT NULL COMMENT '波特率',
  `verification` int DEFAULT NULL COMMENT '校验',
  `road_no` varchar(32)  DEFAULT NULL COMMENT '路段号',
  `national_road_no` int DEFAULT NULL COMMENT '国家高速编号',
  `station_no` varchar(32)  DEFAULT NULL COMMENT '设施编码',
  `direction_no` int DEFAULT NULL COMMENT '方向编码',
  `mile_post` varchar(50)  DEFAULT NULL COMMENT '桩号',
  `mp_value` bigint DEFAULT NULL COMMENT '桩号数值',
  `x` bigint DEFAULT NULL COMMENT '经度',
  `y` bigint DEFAULT NULL COMMENT '纬度',
  `is_active` int DEFAULT NULL COMMENT '是否启用',
  `is_approving` int DEFAULT NULL COMMENT '是否审批中',
  `is_delete` int DEFAULT NULL COMMENT '是否删除',
  `create_time` varchar(30)  DEFAULT NULL COMMENT '保存时间',
  `update_time` varchar(30)  DEFAULT NULL COMMENT '最近更新时间',
  `address` varchar(200)  DEFAULT NULL COMMENT '设备地址码',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`device_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运车检器表，用于记录车检器的设备编号、设备名称、通讯方式、控制协议、IP地址、端口、串口号、波特率、校验方式、路段号、方向编码、桩号、经纬度、设备地址码等基础信息和配置信息，支持查询所有车检器的基础属性、已启用设备、特定型号（如2VD1）设备、特定IP或路段号或方向编码的设备，支持按路段号、方向编码、设施编码、控制协议等多维度统计车检器数量及详细信息，支持设备名称、IP地址、协议类型等字段的精准查询与分析:';
CREATE TABLE `ldly_base_d_weather_detector` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备ID',
  `device_name` varchar(100)  DEFAULT NULL COMMENT '设备名称',
  `device_subtype_no` int DEFAULT NULL COMMENT '类型（设备子类型表)',
  `protocol_flag` int DEFAULT NULL COMMENT '通讯方式',
  `protocol` varchar(50)  DEFAULT NULL COMMENT '控制协议',
  `ip` varchar(100)  DEFAULT NULL COMMENT 'IP地址',
  `port` int DEFAULT NULL COMMENT '端口',
  `com_port` int DEFAULT NULL COMMENT '串口号',
  `com_baud` int DEFAULT NULL COMMENT '波特率',
  `verification` int DEFAULT NULL COMMENT '校验',
  `road_no` varchar(32)  DEFAULT NULL COMMENT '路段号',
  `national_road_no` int DEFAULT NULL COMMENT '国家高速编号',
  `station_no` varchar(32)  DEFAULT NULL COMMENT '设施编码',
  `direction_no` int DEFAULT NULL COMMENT '方向编码',
  `mile_post` varchar(50)  DEFAULT NULL COMMENT '桩号',
  `mp_value` bigint DEFAULT NULL COMMENT '桩号数值',
  `x` bigint DEFAULT NULL COMMENT '经度',
  `y` bigint DEFAULT NULL COMMENT '纬度',
  `is_active` int DEFAULT NULL COMMENT '是否启用',
  `is_approving` int DEFAULT NULL COMMENT '是否审批中',
  `is_delete` int DEFAULT NULL COMMENT '是否删除',
  `create_time` varchar(30)  DEFAULT NULL COMMENT '保存时间',
  `update_time` varchar(30)  DEFAULT NULL COMMENT '最近更新时间',
  `status_address` varchar(100)  DEFAULT NULL COMMENT '状态点位',
  `water_level_address` varchar(100)  DEFAULT NULL COMMENT '水位点位',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`device_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运气象检测器表，用于记录气象检测设备的基础信息、通信配置、位置信息及运行状态，支持按设备编号、名称、类型、通信方式、控制协议、IP地址、端口、串口号、波特率、校验方式、路段号、方向编码、桩号、经纬度等多维度查询，可统计设备总数、启用状态、类型分布及协议使用情况，支持模糊查询和设施编码关联查询，适用于路运气象监测系统的设备管理与数据分析:';
CREATE TABLE `ldly_base_d_wind_detector` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备ID',
  `device_name` varchar(100)  DEFAULT NULL COMMENT '设备名称',
  `update_rate` int DEFAULT NULL COMMENT '更新周期',
  `protocol_flag` int DEFAULT NULL COMMENT '通讯方式',
  `protocol` varchar(50)  DEFAULT NULL COMMENT '控制协议',
  `switch_ratio` double(5,2) DEFAULT NULL COMMENT '量程转换系数',
  `plc_id` varchar(32)  DEFAULT NULL COMMENT '所属PLC',
  `plc_prefix` varchar(100)  DEFAULT NULL COMMENT 'PLC标签',
  `speed_data_tag` varchar(100)  DEFAULT NULL COMMENT '风速值标签',
  `direction_data_tag` varchar(100)  DEFAULT NULL COMMENT '风向值标签',
  `status_tag` varchar(100)  DEFAULT NULL COMMENT '状态标签',
  `com_port` varchar(100)  DEFAULT NULL COMMENT '串口号',
  `baud` int DEFAULT NULL COMMENT '波特率',
  `ip` varchar(100)  DEFAULT NULL COMMENT 'IP地址',
  `port` int DEFAULT NULL COMMENT '端口',
  `io_id` varchar(32)  DEFAULT NULL COMMENT '主控板',
  `road_no` varchar(32)  DEFAULT NULL COMMENT '路段号',
  `national_road_no` int DEFAULT NULL COMMENT '国家高速编号',
  `station_no` varchar(32)  DEFAULT NULL COMMENT '设施编码',
  `direction_no` int DEFAULT NULL COMMENT '方向编码',
  `mile_post` varchar(50)  DEFAULT NULL COMMENT '桩号',
  `mp_value` bigint DEFAULT NULL COMMENT '桩号数值',
  `x` bigint DEFAULT NULL COMMENT '经度',
  `y` bigint DEFAULT NULL COMMENT '纬度',
  `is_active` int DEFAULT NULL COMMENT '是否启用',
  `is_approving` int DEFAULT NULL COMMENT '是否审批中',
  `is_delete` int DEFAULT NULL COMMENT '是否删除',
  `create_time` varchar(30)  DEFAULT NULL COMMENT '保存时间',
  `update_time` varchar(30)  DEFAULT NULL COMMENT '最近更新时间',
  `speed_data_address` varchar(100)  DEFAULT NULL COMMENT '风速值点位',
  `direction_data_address` varchar(100)  DEFAULT NULL COMMENT '风向值点位',
  `speed_status_address` varchar(100)  DEFAULT NULL COMMENT '运行状态点位',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`device_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运风向风速检测器表，用于记录风向风速检测设备的基础信息、通信配置、控制协议、点位信息及运行状态，支持按设备编号、设备名称、所属PLC、路段号、设施编码、方向编码等多维度查询设备详细信息及标签配置，支持统计检测器总数、已启用数量、按所属PLC或协议分类统计，支持查询特定路段、特定协议（如OPC）或特定区域（如九连山往博罗）的设备信息及运行状态，支持查询设备的更新周期、通讯方式、控制协议、量程转换系数、PLC标签、风速值标签、风向值标签、状态标签、串口号、波特率、IP地址、端口、主控板、桩号、经纬度、风速值点位、风向值点位、运行状态点位等详细参数，支持按设施编码、设备名称、方向等条件进行精准查询与统计分析:';
CREATE TABLE `ldly_broadcast_history` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `list_id` varchar(50)  DEFAULT NULL COMMENT '记录编号',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '广播对象ID',
  `station_no` varchar(32)  DEFAULT NULL COMMENT '所属设施ID',
  `operator` varchar(50)  DEFAULT NULL COMMENT '操作人编号',
  `operator_name` varchar(255)  DEFAULT NULL COMMENT '操作人名称',
  `broadcast_time` varchar(30)  DEFAULT NULL COMMENT '广播时间',
  `broadcast_type` int DEFAULT NULL COMMENT '广播类型',
  `broadcast_content` text  COMMENT '广播任务名称',
  `command_id` varchar(50)  DEFAULT NULL COMMENT '指令编号',
  `result` int DEFAULT NULL COMMENT '控制结果',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`list_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运广播历史信息表，用于记录路运广播操作的全过程，包括广播对象ID、所属设施ID、操作人、广播时间、广播类型、广播内容、指令编号、控制结果等关键信息，支持查询所有广播历史记录及实时喊话、广播启动与结束记录，支持按时间、操作人、广播对象、广播类型、控制结果等多维度查询，支持模糊查询广播内容，支持统计广播总数及按类型、结果、操作人、对象分类统计，可查询特定广播对象、操作人、时间段的广播记录，支持查询成功、失败、超时广播记录及最新广播记录，支持分页查询最新100条记录:';
CREATE TABLE `ldly_broadcast_task` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '广播对象ID',
  `is_playing` int DEFAULT NULL COMMENT '是否在播放',
  `broadcast_content` text  COMMENT '广播任务名称',
  `update_time` varchar(30)  DEFAULT NULL COMMENT '更新时间',
  `command_id` varchar(50)  DEFAULT NULL COMMENT '指令编号',
  `broadcast_type` int DEFAULT NULL COMMENT '广播类型',
  `actual_task_id` varchar(50)  DEFAULT NULL COMMENT '实际任务id，停止任务时用到',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`device_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运路运广播当前信息';
CREATE TABLE `ldly_cur_device_data` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备编号',
  `device_type_no` int DEFAULT NULL COMMENT '设备类型编号',
  `time_id` varchar(30)  DEFAULT NULL COMMENT '采集时间',
  `last_update` varchar(30)  DEFAULT NULL COMMENT '入库时间',
  `json_data` text  COMMENT '设备采集cur data，json对象',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`device_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运设备当前信息，用于记录路运设备的实时状态、设备编号、设备类型编号、数据采集时间及入库时间，支持查询所有设备或指定设备的当前数据，统计设备数据记录总数，按设备类型分类统计记录数量，并可获取最新的设备当前数据记录:';CREATE TABLE `ldly_cur_device_status` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备编号',
  `status` int DEFAULT NULL COMMENT '状态类型',
  `error_msg` varchar(255)  DEFAULT NULL COMMENT '故障信息',
  `time_id` varchar(30)  DEFAULT NULL COMMENT '采集时间',
  `last_update` varchar(30)  DEFAULT NULL COMMENT '入库时间',
  `result` int DEFAULT NULL COMMENT '错误码',
  `fault_status_msg` varchar(50)  DEFAULT NULL COMMENT '情报板故障状态信息',
  `fault_status` varchar(1000)  DEFAULT NULL COMMENT '设备状态错误码',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`device_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运当前设备状态表，用于记录路运设备（如摄像枪、火灾报警器、照明、PLC、广播、紧急电话、卷帘门等）的实时状态信息，包括设备编号、状态类型、在线/离线/故障状态、故障信息、错误码、情报板故障状态、采集时间、入库时间等，支持查询所有设备当前状态及详细字段信息，支持按设备编号、状态类型、设备类型等多维度查询在线、离线、故障设备，支持统计设备总数及按状态类型、设备类型分类统计在线、离线、故障设备数量，支持查询最新状态变化及最近上线、离线、故障设备:';
CREATE TABLE `ldly_cur_vms_data` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `list_id` bigint DEFAULT NULL COMMENT 'id，自增长',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备id',
  `text_info` text  COMMENT '发布内容',
  `font_name` varchar(100)  DEFAULT NULL COMMENT '字体',
  `font_size` varchar(100)  DEFAULT NULL COMMENT '字体大小',
  `font_format` varchar(100)  DEFAULT NULL COMMENT '字体左右对齐',
  `font_color` varchar(100)  DEFAULT NULL COMMENT '字体颜色',
  `pic_no` varchar(100)  DEFAULT NULL COMMENT '图片编号',
  `effect` int DEFAULT NULL COMMENT '效果',
  `intervals` int DEFAULT NULL COMMENT '显示时间，播放时间',
  `user_id` varchar(32)  DEFAULT NULL COMMENT '发布人员id',
  `font_format_up_down` varchar(100)  DEFAULT NULL COMMENT '上下对齐',
  `time_id` varchar(30)  DEFAULT NULL COMMENT '采集时间',
  `last_update` varchar(30)  DEFAULT NULL COMMENT '入库时间',
  `pic_id` varchar(50)  DEFAULT NULL COMMENT '图片编号',
  `pic_point_x` int DEFAULT NULL COMMENT '图片坐标x',
  `pic_point_y` int DEFAULT NULL COMMENT '图片坐标y',
  `pic_width` int DEFAULT NULL COMMENT '图片宽',
  `pic_height` int DEFAULT NULL COMMENT '图片高',
  `text_point_x` int DEFAULT NULL COMMENT '文本坐标x',
  `text_point_y` int DEFAULT NULL COMMENT '文本坐标y',
  `text_width` int DEFAULT NULL COMMENT '文本宽',
  `text_height` int DEFAULT NULL COMMENT '文本高',
  `attach_id` varchar(50)  DEFAULT NULL COMMENT '附件编号',
  `user_name` varchar(50)  DEFAULT NULL COMMENT '操作人名称',
  `category_name` varchar(100)  DEFAULT NULL COMMENT '信息分类名称',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`list_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运情报板当前信息表，用于记录情报板的实时发布内容、字体、图片、文本配置、显示效果、发布时间及发布人员等信息，支持按设备编号、发布人员、信息分类等多维度查询，可检索特定内容或最新发布信息，并支持统计信息记录总数及按发布人员、信息分类进行分类统计:';
CREATE TABLE `ldly_his_device_status` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `list_id` bigint DEFAULT NULL COMMENT '编号，自增长',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备编号',
  `status` int DEFAULT NULL COMMENT '状态类型',
  `error_msg` varchar(255)  DEFAULT NULL COMMENT '故障信息',
  `time_id` varchar(30)  DEFAULT NULL COMMENT '采集时间',
  `last_update` varchar(30)  DEFAULT NULL COMMENT '入库时间',
  `result` int DEFAULT NULL COMMENT '错误码',
  `error_status` int DEFAULT NULL COMMENT '故障状态',
  `error_code_list` text  COMMENT '故障码集合',
  `fault_status_msg` varchar(50)  DEFAULT NULL COMMENT '情报板故障状态信息',
  `fault_status` varchar(1000)  DEFAULT NULL COMMENT '设备状态错误码',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`list_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运历史设备状态表，用于记录路运设备的历史运行状态信息，包括设备编号、状态类型（在线/离线/故障）、故障信息、错误码及时间戳，支持按设备编号、时间范围、状态类型等条件查询历史状态记录，支持统计状态记录总数及按状态类型、设备编号分类统计，可查询单设备最新状态及最近故障记录，支持批量查询最新状态数据:';
CREATE TABLE `ldly_his_vms_data` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `list_id` bigint DEFAULT NULL COMMENT '编号，自增长',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备id',
  `text_info` text  COMMENT '发布内容',
  `font_name` varchar(100)  DEFAULT NULL COMMENT '字体',
  `font_size` varchar(100)  DEFAULT NULL COMMENT '字体大小',
  `font_format` varchar(100)  DEFAULT NULL COMMENT '字体左右对齐',
  `font_color` varchar(100)  DEFAULT NULL COMMENT '字体颜色',
  `pic_no` varchar(100)  DEFAULT NULL COMMENT '图片编号',
  `effect` int DEFAULT NULL COMMENT '效果',
  `intervals` int DEFAULT NULL COMMENT '显示时间，播放时间',
  `user_id` varchar(32)  DEFAULT NULL COMMENT '发布人员id',
  `font_format_up_down` varchar(100)  DEFAULT NULL COMMENT '上下对齐',
  `time_id` varchar(30)  DEFAULT NULL COMMENT '采集时间',
  `last_update` varchar(30)  DEFAULT NULL COMMENT '入库时间',
  `pic_id` varchar(50)  DEFAULT NULL COMMENT '图片编号',
  `pic_point_x` int DEFAULT NULL COMMENT '图片坐标x',
  `pic_point_y` int DEFAULT NULL COMMENT '图片坐标y',
  `pic_width` int DEFAULT NULL COMMENT '图片宽',
  `pic_height` int DEFAULT NULL COMMENT '图片高',
  `text_point_x` int DEFAULT NULL COMMENT '文本坐标x',
  `text_point_y` int DEFAULT NULL COMMENT '文本坐标y',
  `text_width` int DEFAULT NULL COMMENT '文本宽',
  `text_height` int DEFAULT NULL COMMENT '文本高',
  `attach_id` varchar(50)  DEFAULT NULL COMMENT '附件编号',
  `extend_id` varchar(36)  DEFAULT NULL COMMENT '单次发布记录id',
  `user_name` varchar(50)  DEFAULT NULL COMMENT '操作人名称',
  `category_name` varchar(100)  DEFAULT NULL COMMENT '信息分类名称',
  `leave_time` varchar(30)  DEFAULT NULL COMMENT '撤离时间',
  `leave_user_name` varchar(50)  DEFAULT NULL COMMENT '信息撤销人名称',
  `approve_user` varchar(50)  DEFAULT NULL COMMENT '审批人',
  `approve_user_name` varchar(50)  DEFAULT NULL COMMENT '审批人姓名',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`list_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运情报板历史信息表，用于记录情报板发布内容、显示配置（字体、图片、文本）、发布与撤离时间、审批与发布人员、信息分类及显示效果等详细信息，支持按设备编号、发布人员、审批人、时间范围、信息内容等多维度查询，支持统计记录总数及按发布人员、信息分类、设备编号分类统计，可查询最新发布记录、最近撤离记录及特定时间段内的发布情况:';
CREATE TABLE `ldly_traffic_device_operation_log` (
  `commonId` bigint NOT NULL AUTO_INCREMENT COMMENT '自增物理主键，不含任何业务含义（主键，支持自增唯一查询），用于数据库内部唯一标识记录、自增主键查询',
  `log_id` varchar(36)  DEFAULT NULL COMMENT '日志编号',
  `log_time` varchar(30)  DEFAULT NULL COMMENT '日志时间',
  `device_id` varchar(32)  DEFAULT NULL COMMENT '设备编号',
  `operation_type_id` int DEFAULT NULL COMMENT '操作类型编号',
  `result` int DEFAULT NULL COMMENT '控制结果',
  `reason` varchar(500)  DEFAULT NULL COMMENT '操作下发失败原因',
  `operator` varchar(36)  DEFAULT NULL COMMENT '操作人编号',
  `remarks` text  COMMENT '操作记录',
  `command_id` varchar(50)  DEFAULT NULL COMMENT '指令编号',
  `over_time` varchar(30)  DEFAULT NULL COMMENT '超时时间',
  `user_name` varchar(50)  DEFAULT NULL COMMENT '操作用户名，或者上级系统名称',
  `result_time` varchar(30)  DEFAULT NULL COMMENT '结果反馈时间',
  `last_update` varchar(30)  DEFAULT NULL COMMENT '更新时间',
  `command_source` int DEFAULT NULL COMMENT '指令来源',
  `data_result` int DEFAULT NULL COMMENT '操作结果',
  `data_result_time` varchar(30)  DEFAULT NULL COMMENT '操作结果时间',
  `is_read` int DEFAULT NULL COMMENT '是否已读',
  `status_result` int DEFAULT NULL COMMENT '状态检测结果',
  PRIMARY KEY (`commonId`) USING BTREE,
  UNIQUE KEY `unique_key` (`log_id`) USING BTREE,
  KEY `idx_commonUpdateTime` (`commonUpdateTime`) USING BTREE
)COMMENT='路段路运设备控制日志表，用于记录设备控制操作的全过程信息，包括日志编号、日志时间、设备编号、操作类型、控制结果、失败原因、操作人、操作记录、指令编号、超时时间、结果反馈时间、指令来源、状态检测结果等，支持按日志时间范围、设备编号、操作人、控制结果、指令来源、操作类型等多维度查询成功、失败、超时、执行中、未读、已读等状态日志，支持查询特定操作记录（如“停止”）、最新日志、最近成功/失败/超时/未读日志，支持按一体化编号、时间范围等条件进行高效检索和分析，支持统计日志总数、按控制结果、指令来源、操作人、设备编号、操作类型分类统计，适用于路运系统对设备控制过程的全面监控与管理:';